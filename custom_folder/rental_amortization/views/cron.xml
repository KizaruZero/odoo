<odoo>

    <record id="ir_cron_rental_amortization_create_vendor_bills" model="ir.cron">
        <field name="name">Rental Amortization: Create Vendor Bills</field>
        <field name="model_id" ref="model_rental_amortization_header"/>
        <field name="state">code</field>
        <field name="code">model.cron_create_vendor_bills()</field>
        <!-- Default: run daily; can be adjusted in UI -->
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active">True</field>
    </record>

</odoo>
